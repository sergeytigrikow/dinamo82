# Starter pipeline

stages:
- stage: 'Prepare'
  displayName: 'Prepare'

  jobs:
  - job: 'First_Job'
    displayName: 'Build pipeline NetFramework Yaml'
    pool:
      name: Default
    steps:
    - script: echo Hello, world!
    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(Build.BinariesDirectory)'
        includeRootFolder: true
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
        replaceExistingArchive: true
    
  - job: 'Second_Job'
    displayName: 'Second_Job Build pipeline NetFramework Yaml'
    pool:
      name: Default
    steps:
    - script: echo Hello, world!
  
